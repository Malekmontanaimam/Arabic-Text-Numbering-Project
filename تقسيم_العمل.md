# تقسيم العمل - مشروع ترقيم النص العربي



## 1️⃣ ملف: `data_loader.py` (2 دوال)

### الدالة 1: `load_punctuation_data`
**يأخذ:** `data_path` (مسار الملف)  
**يرجع:** `df`, `stats`, `punct_dist`  
**المسؤول:** تحميل البيانات من ملف zip

---

### الدالة 2: `create_training_sequences`
**يأخذ:** `texts` (قائمة نصوص), `max_length=128`  
**يرجع:** `X_sequences`, `y_labels`, `word_to_idx`  
**المسؤول:** تحويل النصوص إلى sequences للتدريب

---

## 2️⃣ ملف: `preprocessing.py` (4 دوال)

### الدالة 1: `normalize_punctuation`
**يأخذ:** `text` (نص)  
**يرجع:** `normalized_text` (نص موحد)  
**المسؤول:** توحيد علامات الترقيم

---

### الدالة 2: `map_punctuation_to_labels`
**يأخذ:** لا شيء  
**يرجع:** `punct_to_label`, `label_to_punct` (قاموسان)  
**المسؤول:** إنشاء mapping بين علامات الترقيم والأرقام

---

### الدالة 3: `preprocess_for_punctuation`
**يأخذ:** `df` (DataFrame), `text_column='text'`  
**يرجع:** `df_processed` (DataFrame)  
**المسؤول:** معالجة مسبقة للنصوص

---

### الدالة 4: `remove_punctuation_for_training`
**يأخذ:** `text` (نص)  
**يرجع:** `text_without_punct` (نص بدون ترقيم)  
**المسؤول:** إزالة علامات الترقيم

---

## 3️⃣ ملف: `models.py` (2 دوال)

### الدالة 1: `build_sequence_model`
**يأخذ:** `model_type`, `vocab_size`, `embedding_dim`, `lstm_units`, `num_classes`, `max_length`, `dropout`, `**params`  
**يرجع:** `model` (نموذج Keras)  
**المسؤول:** بناء نماذج LSTM/GRU/Transformer

---

### الدالة 2: `build_transfer_learning_model`
**يأخذ:** `pretrained_model_name`, `num_classes`, `max_length`, `dropout`  
**يرجع:** `model`, `tokenizer`  
**المسؤول:** بناء نموذج BERT

---

## 4️⃣ ملف: `training.py` (2 دوال)

### الدالة 1: `train_punctuation_model`
**يأخذ:** `model`, `X_train`, `y_train`, `X_val`, `y_val`, `epochs=10`, `batch_size=32`, `learning_rate=0.001`, `use_pretrained=False`, `tokenizer=None`, `**params`  
**يرجع:** `model`, `history`, `training_time`  
**المسؤول:** تدريب النموذج

---

### الدالة 2: `prepare_data_for_bert`
**يأخذ:** `texts`, `labels`, `tokenizer`, `max_length=128`  
**يرجع:** `input_ids`, `attention_mask`, `labels_processed`  
**المسؤول:** تحضير بيانات BERT

---

## 5️⃣ ملف: `evaluation.py` (5 دوال)

### الدالة 1: `evaluate_punctuation_model`
**يأخذ:** `model`, `X_test`, `y_test`, `model_info`, `use_pretrained=False`, `tokenizer=None`, `max_length=128`  
**يرجع:** `results_dict` (قاموس النتائج)  
**المسؤول:** تقييم النموذج

---

### الدالة 2: `predict_punctuation`
**يأخذ:** `model`, `text`, `word_to_idx`, `label_to_punct`, `use_pretrained=False`, `tokenizer=None`, `max_length=128`  
**يرجع:** `punctuated_text` (نص مع ترقيم)  
**المسؤول:** التنبؤ بنص جديد

---

### الدالة 3: `evaluate_best_model` ⭐ **مهمة للتسليم**
**يأخذ:** `model_path`, `test_data_path`, `word_to_idx_path=None`, `model_type='standard'`, `tokenizer_path=None`  
**يرجع:** `accuracy`, `fscore`, `predictions`  
**المسؤول:** التقييم النهائي + طباعة النتائج

---

### الدالة 4: `visualize_punctuation_results`
**يأخذ:** `results_dict`  
**يرجع:** `figures` (قائمة أشكال)  
**المسؤول:** رسم النتائج

---

### الدالة 5: `create_comparison_table`
**يأخذ:** `all_results` (قائمة نتائج)  
**يرجع:** `comparison_df` (DataFrame)  
**المسؤول:** جدول مقارنة النماذج

---

## 📊 إجمالي الدوال: 15 دالة

- `data_loader.py`: 2 دوال
- `preprocessing.py`: 4 دوال
- `models.py`: 2 دوال
- `training.py`: 2 دوال
- `evaluation.py`: 5 دوال

---

## ⚠️ ملاحظات مهمة

1. **الدالة الأهم:** `evaluate_best_model` - مطلوبة للتسليم
2. **التسميات:** 0-6 (0=لا يوجد، 1=فاصلة، 2=نقطة، 3=استفهام، 4=فاصلة منقوطة، 5=نقطتان، 6=تعجب)
3. **التوافق:** تأكد من أن الدوال متوافقة مع بعضها (مثل word_to_idx)

---

